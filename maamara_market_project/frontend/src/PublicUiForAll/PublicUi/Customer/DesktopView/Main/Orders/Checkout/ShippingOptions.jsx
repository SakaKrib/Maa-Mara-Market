import React from "react";
export default function ShippingOptions({ options=[], selectedShipping, onSelect, error }) {
 return <section className="space-y-3"><h2 className="font-semibold">Shipping</h2><p className="text-sm text-gray-500">Select a live DHL or FedEx rate.</p>{options.map((o,i)=>{const selected=selectedShipping?.provider===o.provider&&selectedShipping?.service===o.service&&selectedShipping?.currency===o.currency&&String(selectedShipping?.price)===String(o.price);return <button type="button" key={String(o.provider)+"-"+String(o.service)+"-"+i} onClick={()=>onSelect(o)} className={"w-full text-left rounded-xl border p-4 "+(selected?"border-blue-600 ring-2 ring-blue-100":"border-gray-200")}><div className="flex justify-between gap-4"><div><b>{o.provider}</b><div className="text-sm text-gray-600">{o.service||"Standard service"}</div></div><b>{o.currency} {o.price}</b></div></button>})}{error?.message&&<p className="text-sm text-red-600">{error.message}</p>}</section>;
}